//Tipos de saída de escrita no JavaScrip(JS)


    //usa o ID no html e passa a mensagem no JS

        document.getElementById("").innerHTML="";

    //usa o <script> no html e passa a mensagem ou operação com números

        document.write();

    //Caixa de texto

        alert("");

    //Parecido com alert mas é mais silecioso(Aparece na parte console do F12 da página)

        console.log();




//Variáveis


    /*var(Variavel que pode ser redeclarada) pode atribuir valores as palavras, tanto str quanto numeros e
    realizar soma entre eles,tendo as suas regras para isso. coringa*/

        var a,b,c;                      var nome,sobrenome,completo;
        a=3;                            nome="Filipe"            
        b=2;                            sobrenome="Lopes"    
        c=a+b;                          completo=nome + " " + sobrenome

    //let(Variável única) não pode ter seu valor reatribuido por outro let dentro do seu bloco. É o mais usado.

        let a="JOAO"
        let a="VICTOR"
        XXXX//Erro

    /*const(não pode mudar) não pode ter seu valor reatribuido por outro const, independente do bloco que 
    estiver vai ser o primeiro que vai ser pego. Mais restrito.*/

        const c = 3
        //bloco de código
        {
            const c =1
        }   
        //c = 3




//Operadores


    // Operadores Aritméticos(+,-,*,/,++,--): Matemáticos, encremento e decremento.

        a+b; a-b; a*b; a/b; a = ++b; --a;
    
    // Operadores de Atribuição(=,+=,-=,*=,/=)
    
        a = b * c; a += b; a -= b; a *= b; a /= b;
    
    // Operadores de Sequência(+ para textos)

        var p1,p2, TOTAL;
        p1= "FILIPE";   
        p2= "LOPES";  
        TOTAL = p1 + p2;

    // Operadores Comparação(==,===,!=): Faz comparações entre os valores
        
        var l1,l2,l3;              var l1,l2,l3;                        var l1,l2,l3;
        l1=4;                      l1=4;                                l1=4;
        l2=3;                      l2=3;                                l2=3;
        l3=(l1 == l2);             l3=(l1 === l2);                      l3=(l1 != l2);
        //true ou false         //true se for o mesmo numero e          //true ou false pela diferença
        //se for igual          //pertencer a mesma classe(str ou num)

    // Operadores Condicional e ternário(<,>,<=,>=,if,else)

        var n1,n2,n3;              var n1,n2,n3;                var idade, eleitor;
        n1=2;                      n1=2;                        idade=18;
        n2=3;                      n2=3;                        eleitor = (idade < 18) ? "Não, Eleitor" : "Sim, Eleitor"
        n3=(n1 < n2);       n3=(n1 <= n2);

    // Operadores Lógicos(&&,||,!)
        
        resultado1 = (idad > 60 && idad < 70); //e
        resultado2 = (idad > 10 || idad < 70); //ou
        resultado3 = !(idad === 14); //negação 
        resultado4 = (idad === 18 && idad === 70); //e especifico
        resultado5 = (idad === 18 || idad === 70); //ou especifico


    

//Funções


    //tag funcion serve para diversos casos(é um def)
        
        //funcion de soma
        function soma(valor1,valor2){
            return valor1 + valor2;
        }

        //funcion de multiplicação e etc
        function realParaDolar (real,cotacaoDolar) {    
        return real * cotacaoDolar;
        }
        var totl = realParaDolar(10, 5.08);

        //formas de saída
        document.getElementById("e").innerHTML = soma(10,10);
        //ou
        var totas = soma(20,13);
        console.log(totas);
        //ou
        var tot = soma(1,2);
        alert("a soma é" + tot)

        //outras coisas
        
        function alertHello(){                          function alertHello(){                          
        alert("ola mundo");                             alert("ola mundo");
        }                                               }
        alertHello();                           /*html*/<button onclick="alertHello()">Clique aqui</button>;
        //A função pode ser chamada tanto no codigo js, tanto no html atraves de butão.

        function minhaFuncao(){
        var i = 2; //variavel local(Funciona apenas dentro do corpo da função)
        }    

//Objetos

    const carro = {
    marca:"ford", 
    modelo:"ka", 
    ano:2015, 
    placa:"ABC-1234",
    }
    //objeto literal(ja tem seus valores)

        //metodos
        
        const carro = {
        marca:"ford", 
        modelo:"ka", 
        ano:2015, 
        placa:"ABC-1234",
        buzina: function(){alert("Biiiiiiiiiiii")},
        completo: function(){
            return "A marca é "+this.marca+" e o modelo é: " +this.modelo}
        };
        //metodos são as funções dentro da definição de objetos

        console.log(carro);
        console.log(carro.placa);
        console.log(carro["marca"]);
        carro.buzina();
        console.log(carro.completo);
        //formas de puxar uma saida especifica ou não 


    

//Eventos(PARTE MAIS IMPORTANTE PARA INTERATIVIDADE)    


        //Onclick(disparado quando clicado)
        
        

        //Ondblclick



        //onmouseover



        //onmouseout


        
        //onmousemove
        


        //onmousedown



        //onmouseuo



        //onfocus



        //onchange


        
        //onblur



        //onkeydown



        //onkeypress



        //onkeyup



        //onload



        //onresize